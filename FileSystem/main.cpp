#include <iostream>
#include "Consola.h"

int main() {
    // Crear Disco -----
    // Crear RAID ------
    // Crear Particion (Primaria / Logica)
    // Mount
    // Mkfs
    // Crear Carpeta (mkdir)
    // Crear Archivo (mkfile)
    // Grafico del arbol;

    Consola consola;
    consola.ejecutar();

    return 0;
}
